<h1>BURGERS!!!</h1>

<ol class="breadcrumb">
  <li class="breadcrumb-item active">Home</li>
</ol>
<ol class="breadcrumb">
  <li class="breadcrumb-item"><a href="#">Home</a></li>
  <li class="breadcrumb-item active">Library</li>
</ol>


<h2>BURGERS 4 U 2 EAT</h2>

<ul class="breadcrumb">
    {{#each burgers}}
      {{#unless devoured}}
       <li class="breadcrumb-item active" >{{this.id}}. {{this.burger_name}} </li>
        {{/unless}}
        {{/each}}
</ul>

<h2>BURGERS U ATE ALREADY!</h2>

<ul>
{{#each burgers}}
{{#if devoured}}
{{this.id}}. {{this.burger_name}}
{{/if}}
{{/each}}
</ul>

<h2>Create a burga!</h2>
<form class="create-form">

    <div class= "form-group">
        <label for="newBurg">Burger name:</label>
        <input type="text" id="newBurg" name="name">
    </div>
<div class="form-group">
    <label for="devoured">Devoured?</label>
    
    <br>
    <input type="radio" name="devoured" value="1" checked>Devoured!
    <br>
    <input type="radio" name="devoured" value="0"> Burgers 4 u 2 eat!
</div>

<button type="submit">Add a Burger</button>

</form>